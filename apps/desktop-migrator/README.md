# desktop-migrator (placeholder en monorepo)

La implementación vive en un repo **hermano**: **`alega-desktop`** (Electron + Vue 3 + PrimeVue), junto a `friendly-succotash/`. Este directorio se mantiene como recordatorio / futura integración en workspace.

Ver: [alega-desktop README](../../../alega-desktop/README.md) y [contrato API](../../docs/desktop-agent-contract.md) / [blueprint](../../docs/desktop-agent-blueprint.md).

## Alternativa web

Vista **Migración** (`/import`) con ZIP o TUS.
