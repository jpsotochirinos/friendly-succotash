# Onboarding Estudio Mariscal (v2)

- Configurar `sinoePolicy` por defecto en `organizations.feature_flags` (SQL o panel futuro) según el estudio.
- Tras el primer login, completar el wizard de onboarding existente; opcional: fijar `onboardingState.mariscalV2` en localStorage para forzar un tour (implementación UI futura).
- Navegación: Bandeja SINOE en el sidebar; Blueprints bajo Ajustes → “Blueprints de procedimiento”.
