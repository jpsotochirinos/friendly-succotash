import { Editor } from '../../core/Editor.js';
import { Query, QueryResult, UnknownNodeDiagnostic } from '@superdoc/document-api';
import { BlockIndex } from '../helpers/node-address-resolver.js';
/**
 * Executes an inline-level node selector against the inline index.
 *
 * @param editor - The editor instance.
 * @param index - Pre-built block index (used for within-scope resolution).
 * @param query - The query with an inline node selector.
 * @param diagnostics - Mutable array to collect diagnostics into.
 * @returns Paginated query result containing inline-kind matches.
 */
export declare function executeInlineSelector(editor: Editor, index: BlockIndex, query: Query, diagnostics: UnknownNodeDiagnostic[]): QueryResult;
//# sourceMappingURL=inline-strategy.d.ts.map