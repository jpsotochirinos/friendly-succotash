import { Editor } from '../../Editor.js';
import { MutatePartResult } from '../types.js';
import { NumberingModel } from './numbering-transforms.js';
/**
 * Run a single numbering transform inside a `mutatePart` transaction.
 *
 * The `transform` callback receives the live `NumberingModel` from
 * `converter.numbering`. It should mutate in-place and return any
 * values the caller needs (e.g., allocated numId).
 */
export declare function mutateNumbering<TResult = void>(editor: Editor, source: string, transform: (numbering: NumberingModel) => TResult, options?: {
    dryRun?: boolean;
    expectedRevision?: string;
}): MutatePartResult<TResult>;
/**
 * Run multiple numbering transforms in a single `mutateParts` transaction.
 *
 * Only one clone, one revision increment, one `afterCommit`. Use this for
 * paste/conversion operations that create many definitions at once.
 */
export declare function mutateNumberingBatch<TResult = void>(editor: Editor, source: string, transforms: Array<(numbering: NumberingModel) => unknown>, options?: {
    dryRun?: boolean;
    expectedRevision?: string;
}): {
    changed: boolean;
    degraded: boolean;
};
//# sourceMappingURL=numbering-mutation.d.ts.map