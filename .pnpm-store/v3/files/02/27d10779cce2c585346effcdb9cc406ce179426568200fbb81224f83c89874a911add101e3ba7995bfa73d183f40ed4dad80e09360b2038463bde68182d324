/**
 * Synchronize OPC package metadata with the final set of output entries.
 *
 * Reads the current [Content_Types].xml and _rels/.rels, reconciles them
 * against the managed parts registry, and returns corrected XML strings
 * for both files.
 *
 * @param {PackageEntrySource} source - Layered package entry view
 * @returns {SyncResult}
 */
export function syncPackageMetadata({ baseFiles, updatedDocs }: PackageEntrySource): SyncResult;
export type ManagedPartEntry = {
    /**
     * - Path inside the zip (e.g. "docProps/custom.xml")
     */
    zipPath: string;
    /**
     * - Required Override ContentType value
     */
    contentType: string;
    /**
     * - Required root Relationship Type URI
     */
    relationshipType: string;
};
export type PackageEntrySource = {
    /**
     *   Original package entries — either an array of {name, content} or a key→content map.
     */
    baseFiles: Array<{
        name: string;
        content: string;
    }> | Record<string, string> | null;
    /**
     *   Export-time overrides. A null value means the entry is deleted.
     */
    updatedDocs: Record<string, string | null>;
};
export type SyncResult = {
    /**
     * - Reconciled [Content_Types].xml
     */
    contentTypesXml: string;
    /**
     * - Reconciled _rels/.rels
     */
    relsXml: string;
};
//# sourceMappingURL=sync-package-metadata.d.ts.map