import { PartId, PartDescriptor } from '../types.js';
interface MockConverterOptions {
    convertedXml?: Record<string, unknown>;
    documentModified?: boolean;
    documentGuid?: string | null;
}
/** Creates a minimal mock editor with a converter suitable for parts system testing. */
export declare function createTestEditor(opts?: MockConverterOptions): {
    converter: {
        convertedXml: Record<string, unknown>;
        documentModified: boolean;
        documentGuid: string;
        promoteToGuid(): string;
    };
    options: {
        collaborationProvider: null;
        ydoc: null;
    };
    state: {
        tr: {};
    };
    view: undefined;
    on(name: string, fn: (...args: unknown[]) => void): void;
    emit(name: string, ...args: unknown[]): void;
    safeEmit(name: string, ...args: unknown[]): Error[];
    off(name: string, fn?: (...args: unknown[]) => void): void;
    /** Test-only: get all listeners for an event. */
    getListeners(name: string): ((...args: unknown[]) => void)[];
};
/** Registers a part into the test editor's convertedXml. */
export declare function withPart(editor: ReturnType<typeof createTestEditor>, partId: PartId, data: unknown): void;
/** Registers a minimal part descriptor for testing. */
export declare function withDescriptor<TPart>(descriptor: PartDescriptor<TPart>): void;
/** Cleans up all test registrations. Call in afterEach. */
export declare function cleanupParts(): void;
/**
 * Patch an existing mock editor to work with the parts system.
 *
 * Adds `convertedXml`, `documentModified`, `documentGuid`, and `safeEmit`
 * if missing. Also populates `convertedXml['word/numbering.xml']` from
 * the mock's `converter.numbering` if present.
 */
export declare function patchMockForParts(mockEditor: any): void;
export {};
//# sourceMappingURL=test-helpers.d.ts.map