import { ProofingAnnotation } from '../types.js';
/**
 * Apply proofing decorations to the rendered DOM.
 *
 * Returns `true` when the DOM was mutated and the caller should rebuild any
 * PM-position index derived from the painted surface.
 */
export declare function applyProofingDecorations(container: HTMLElement, annotations: ProofingAnnotation[]): boolean;
/**
 * Remove all proofing decorations from the container and restore split spans.
 */
export declare function clearProofingDecorations(container: HTMLElement): boolean;
//# sourceMappingURL=decoration-pass.d.ts.map